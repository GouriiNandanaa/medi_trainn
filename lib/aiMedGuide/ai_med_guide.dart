import 'package:flutter/material.dart';

class HealthAssistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("HealthAssist 24/7")),
      body: Center(child: Text("HealthAssist Page")),
    );
  }
}