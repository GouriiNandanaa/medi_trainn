import 'package:flutter/material.dart';
//


class AIMedGuidePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AI MedGuide")),
      body: Center(child: Text("AI MedGuide Page")),
    );
  }
}