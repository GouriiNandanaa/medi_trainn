import 'package:flutter/material.dart';

class TrackMyMedsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("TrackMyMeds")),
      body: Center(child: Text("TrackMyMeds Page")),
    );
  }
}
