import 'package:flutter/material.dart';

class StationCarePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("StationCare")),
      body: Center(child: Text("StationCare Page")),
    );
  }
}