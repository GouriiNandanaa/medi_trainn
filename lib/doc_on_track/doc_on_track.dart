import 'package:flutter/material.dart';

class DocOnTrackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("DocOnTrack")),
      body: Center(child: Text("DocOnTrack Page")),
    );
  }
}