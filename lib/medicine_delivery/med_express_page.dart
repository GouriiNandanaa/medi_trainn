
import 'package:flutter/material.dart';

class MedExpressPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("MedExpress")),
      body: Center(child: Text("MedExpress Page")),
    );
  }
}